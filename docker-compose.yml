version: "3.9"

services:
  resume-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-resume-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
